void main(){
  var city = ['Dhaka','Rangpur','Khulna',10,true];  // declaring list
//   calling first element
  print(city.first);
  // calling hashcode
  print(city.hashCode);
  // last entry
  print(city.last);
  // length
  print(city.length);
// reverse
  print(city.reversed);
  // cheking empty
  print(city.isEmpty);
  print(city.isNotEmpty);
  // rumtime type
  print(city.runtimeType);
  // first or null
  var citynull = [];
  print(citynull.firstOrNull);

// single
  var vill = ['Ashulia'];
  print(vill.single);

}